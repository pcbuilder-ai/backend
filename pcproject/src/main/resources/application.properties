spring.application.name=pcproject

# ===================================================================
# ??? ?? (Docker? MySQL? ??? ??)
# ===================================================================
# ?? ??: jdbc:mysql://localhost:3306/project
# Docker?? ???: jdbc:mysql://db:3306/project
# Docker 실행 시 기본값은 db 호스트로 연결하고, 환경변수로 재정의 가능하도록 설정
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://db:3306/project?serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false&characterEncoding=UTF-8&useUnicode=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:1234}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================================================
# Docker Compose?? ??? ?? (??? ??)
# ===================================================================
# spring.datasource.url=${SPRING_DATASOURCE_URL}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# JPA/Hibernate ??
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# UTF-8 인코딩 설정
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force-response=true
server.servlet.encoding.force=true

# Logging
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG

# OpenAI API
openai.api.key=${OPENAI_API_KEY:}
openai.api.base=https://api.openai.com
openai.api.model=gpt-4o-mini
openai.org=${OPENAI_ORG:}
openai.project=${OPENAI_PROJECT:}
